# ============================================
# wrangler.toml - Cloudflare Worker 설정 파일
# Campus Smart Hub API
# ============================================

name = "campus-smart-hub-api"
main = "api/worker.js"
compatibility_date = "2024-01-01"

# 개발 서버 설정
[dev]
port = 8787

# 환경별 설정 (필요시)
# [env.production]
# routes = [{ pattern = "api.your-domain.com/*", zone_name = "your-domain.com" }]
